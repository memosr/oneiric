# Skill: dream-analyst

Extracts structured metadata from a raw dream transcript — symbols, mood, narrative arc, color palette, and visual motifs — and returns a `DreamAnalysis` JSON object for downstream pipeline stages.
